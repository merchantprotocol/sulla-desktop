; @langchain/community -> @browserbasehq/stagehand peers openai@^4 while the
; tree resolves openai@6; npm refuses the tree in strict peer mode. Yarn (the
; primary manager, see packageManager) tolerates it. Keep npm installs
; deterministic instead of requiring everyone to pass the flag by hand.
legacy-peer-deps=true
